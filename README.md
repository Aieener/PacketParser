# PacketParser
This is the final Project from course CIS 553: Networked systems at Upenn, where we have wrote a program to parse and interpret captured Ethernet traffic containing
IP datagrams (UDP and TCP), and stores captured email messages sent in that traffic into files. (Using C++)
